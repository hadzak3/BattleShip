package controller;

import java.util.ArrayList;

//TODO Mock object of GameController + Mock object of Model
/*public class MockGameController extends GameController{
	private static int playersCount = Constants.N_PLAYERS;
	private static ArrayList<Player> players;
	private static Board board;
	//mock object setup
	MockGameController(){
		board = new Board(Constants.N_BOARD_ROWS_CELLS, Constants.N_BOARD_ROWS_CELLS);
		OneCellShip ocs = new OneCellShip();
		board.createShip(0, 0, Constants.SHIP_HORIZONTAL, ocs);
		Player player = new Player("JUGADOR1", board);
		players.add(player);
		
		board = new Board(Constants.N_BOARD_ROWS_CELLS, Constants.N_BOARD_ROWS_CELLS);
		board.createShip(0, 0, Constants.SHIP_HORIZONTAL, ocs);
	}
}*/
